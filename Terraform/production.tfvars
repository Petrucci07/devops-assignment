psql_server.name = "postgresql-server-cliient1"
psql_server.location = "west-europe"

# some variablew can be used as defults so is commented out to point that is not in use(could be removed), where in test env we might need a different id
# subscription_id = "aaaaaaa-bbb-ccccc-ccc-dddddddd"
